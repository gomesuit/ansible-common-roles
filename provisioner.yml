---
- hosts: localhost
  sudo: yes
  roles:
    - common
#    - serverspec
    - bundler
    - docker
  tasks:
    - name: bundle install for provisioner
      command: chdir=/home/vagrant/provisioner /usr/local/ruby/bin/bundle
